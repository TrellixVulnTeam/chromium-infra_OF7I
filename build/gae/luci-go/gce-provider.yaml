name: luci-go/gce-provider
extends: ../base.yaml

inputsdir: ../../../go/src/go.chromium.org/luci/gce/appengine

build:
  - go_gae_bundle: ${inputsdir}/frontend/app.yaml
    dest: ${contextdir}/frontend
