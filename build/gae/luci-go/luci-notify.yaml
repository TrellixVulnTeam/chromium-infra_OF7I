name: luci-go/luci-notify
extends: ../base.yaml

inputsdir: ../../../go/src/go.chromium.org/luci/luci_notify

build:
  - go_gae_bundle: ${inputsdir}/frontend/app.yaml
    dest: ${contextdir}/frontend
